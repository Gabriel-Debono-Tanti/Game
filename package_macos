#!/bin/sh

package=Game.app/Game.app
MacOS=$package/Contents/MacOS
mkdir -p $MacOS
chmod +x package_macos
cp ./Game $MacOS

mkdir -p $package/Contents/libs/
dylibbundler -od -b -ns -x ./Game -d $package/Contents/libs/
